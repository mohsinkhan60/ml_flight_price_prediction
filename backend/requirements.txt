Flask>=2.0
flask-cors>=3.0
scikit-learn>=1.0
numpy>=1.21
pandas>=1.3
# HTTP client used to download the model at startup when MODEL_URL is set
requests>=2.28
# Optional / recommended for production
# gunicorn>=20.1.0
